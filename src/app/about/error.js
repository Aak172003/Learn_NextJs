"use client"
// with above line this component is become client side component 

export default function pageError({ error, reset }) {
    return (
        <div>
            <h1>Error is Coming in About Route</h1>
        </div>

    )
}
