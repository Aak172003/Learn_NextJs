import React from "react";
import LoginPage from "./loginPage";
export const metadata = {
    title: "Login Page"
}
const page = () => {
    return (
        <LoginPage />
    )
}

export default page